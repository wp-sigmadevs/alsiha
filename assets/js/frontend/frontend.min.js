/******/ (function() { // webpackBootstrap
/******/ 	var __webpack_modules__ = ({

/***/ "./src/js/frontend/classes/SigmaFrontend.js":
/*!**************************************************!*\
  !*** ./src/js/frontend/classes/SigmaFrontend.js ***!
  \**************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   SigmaFrontend: function() { return /* binding */ SigmaFrontend; }
/* harmony export */ });
/* harmony import */ var _components_imports__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../components/imports */ "./src/js/frontend/components/imports.js");
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * A class that handles frontend functionality.
 *
 * @class SigmaFrontend
 */


var SigmaFrontend = /*#__PURE__*/function () {
  /**
   * Constructor for SigmaFrontend class.
   * Initializes and imports necessary for functionality.
   */
  function SigmaFrontend() {
    var _this = this;
    _classCallCheck(this, SigmaFrontend);
    /**
     * jQuery instance for DOM manipulation.
     *
     * @type {jQuery}
     */
    _defineProperty(this, "$", jQuery);
    /**
     * Initialize imported functions.
     *
     * @function
     */
    _defineProperty(this, "initializeImports", function () {
      _this.initVars = _components_imports__WEBPACK_IMPORTED_MODULE_0__.initVars;
      _this.headerSearchAction = _components_imports__WEBPACK_IMPORTED_MODULE_0__.headerSearchAction;
      _this.headerPlaceholderSpace = _components_imports__WEBPACK_IMPORTED_MODULE_0__.headerPlaceholderSpace;
      _this.topBannerHeight = _components_imports__WEBPACK_IMPORTED_MODULE_0__.topBannerHeight;
      _this.intelligentHeader = _components_imports__WEBPACK_IMPORTED_MODULE_0__.intelligentHeader;
    });
    /**
     * Initialize vars using initVars.
     *
     * @function
     */
    _defineProperty(this, "initializeVars", function () {
      _this.vars = _this.initVars(_this.$);
    });
    /**
     * Initializes the SigmaFrontend class when the DOM is ready.
     *
     * @function
     */
    _defineProperty(this, "domReady", function () {
      _this.headerSearchAction(_this.$, _this.vars);
      _this.headerPlaceholderSpace(_this.$, _this.vars);
      _this.topBannerHeight(_this.$, _this.vars);
      _this.intelligentHeader(_this.$, _this.vars);
    });
    this.initializeImports();
    this.initializeVars();
  }
  return _createClass(SigmaFrontend, [{
    key: "domLoad",
    value:
    /**
     * Initializes the SigmaFrontend class when the DOM is loaded.
     *
     * @function
     */
    function domLoad() {
      console.log('load');
    }

    /**
     * Initializes the SigmaFrontend class when the DOM is resized.
     *
     * @function
     */
  }, {
    key: "domResize",
    value: function domResize() {
      this.headerPlaceholderSpace(this.$, this.vars);
      this.topBannerHeight(this.$, this.vars);
    }
  }]);
}();

/***/ }),

/***/ "./src/js/frontend/components/headerPlaceholderSpace.js":
/*!**************************************************************!*\
  !*** ./src/js/frontend/components/headerPlaceholderSpace.js ***!
  \**************************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   headerPlaceholderSpace: function() { return /* binding */ headerPlaceholderSpace; }
/* harmony export */ });
/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../utils */ "./src/js/frontend/utils.js");
/**
 * Handles the header search functionality, including opening the search modal and
 * closing it when clicking on the overlay or close button.
 *
 * @param {Object} $    - The jQuery object.
 * @param {Object} vars - An object containing the necessary variables and elements.
 */


var headerPlaceholderSpace = function headerPlaceholderSpace($, vars) {
  if (!(0,_utils__WEBPACK_IMPORTED_MODULE_0__.elementExists)(vars.intelHeader)) {
    return false;
  }
  var intHeight = vars.intelHeader[0].getBoundingClientRect().height;
  vars.headerSpace.height(intHeight);
};

/***/ }),

/***/ "./src/js/frontend/components/headerSearchAction.js":
/*!**********************************************************!*\
  !*** ./src/js/frontend/components/headerSearchAction.js ***!
  \**********************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   headerSearchAction: function() { return /* binding */ headerSearchAction; }
/* harmony export */ });
/**
 * Handles the header search functionality, including opening the search modal and
 * closing it when clicking on the overlay or close button.
 *
 * @param {Object} $    - The jQuery object.
 * @param {Object} vars - An object containing the necessary variables and elements.
 */

var headerSearchAction = function headerSearchAction($, vars) {
  // Trigger the search modal
  vars.headerSearchTrigger.on('click', function (event) {
    event.preventDefault();
    vars.headerSearchModal.add(vars.overlay).addClass('open');
    vars.headerSearchModal.find('input[type="search"]').focus();
  });

  // Handle click on the overlay or close button to close the modal
  $(vars.overlay).add(vars.headerSearchModal.find('button.close')).on('click', function (event) {
    if ($(event.currentTarget).hasClass('open') || event.currentTarget.className === 'close') {
      vars.headerSearchModal.add(vars.overlay).removeClass('open');
    }
  });
};

/***/ }),

/***/ "./src/js/frontend/components/imports.js":
/*!***********************************************!*\
  !*** ./src/js/frontend/components/imports.js ***!
  \***********************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   headerPlaceholderSpace: function() { return /* reexport safe */ _headerPlaceholderSpace__WEBPACK_IMPORTED_MODULE_2__.headerPlaceholderSpace; },
/* harmony export */   headerSearchAction: function() { return /* reexport safe */ _headerSearchAction__WEBPACK_IMPORTED_MODULE_1__.headerSearchAction; },
/* harmony export */   initVars: function() { return /* reexport safe */ _variables__WEBPACK_IMPORTED_MODULE_0__.initVars; },
/* harmony export */   intelligentHeader: function() { return /* reexport safe */ _intelligentHeader__WEBPACK_IMPORTED_MODULE_4__.intelligentHeader; },
/* harmony export */   topBannerHeight: function() { return /* reexport safe */ _topBannerHeight__WEBPACK_IMPORTED_MODULE_3__.topBannerHeight; }
/* harmony export */ });
/* harmony import */ var _variables__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./variables */ "./src/js/frontend/components/variables.js");
/* harmony import */ var _headerSearchAction__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./headerSearchAction */ "./src/js/frontend/components/headerSearchAction.js");
/* harmony import */ var _headerPlaceholderSpace__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./headerPlaceholderSpace */ "./src/js/frontend/components/headerPlaceholderSpace.js");
/* harmony import */ var _topBannerHeight__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./topBannerHeight */ "./src/js/frontend/components/topBannerHeight.js");
/* harmony import */ var _intelligentHeader__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./intelligentHeader */ "./src/js/frontend/components/intelligentHeader.js");
/**
 * Module for managing imports in the project.
 *
 * This module centralizes all import statements for better organization
 * and clarity in the main project files.
 *
 * @module imports
 */

// Import statements for main file






// Export all imported items


/***/ }),

/***/ "./src/js/frontend/components/intelligentHeader.js":
/*!*********************************************************!*\
  !*** ./src/js/frontend/components/intelligentHeader.js ***!
  \*********************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   intelligentHeader: function() { return /* binding */ intelligentHeader; }
/* harmony export */ });
/* harmony import */ var headroom_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! headroom.js */ "./node_modules/headroom.js/dist/headroom.js");
/* harmony import */ var headroom_js__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(headroom_js__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../utils */ "./src/js/frontend/utils.js");
/**
 * Handles the header search functionality, including opening the search modal and
 * closing it when clicking on the overlay or close button.
 *
 * @param {Object} $    - The jQuery object.
 * @param {Object} vars - An object containing the necessary variables and elements.
 */



var intelligentHeader = function intelligentHeader($, vars) {
  if (!(0,_utils__WEBPACK_IMPORTED_MODULE_1__.elementExists)(vars.intelHeader)) {
    return false;
  }
  var options = {
    offset: {
      up: 50,
      down: 100
    },
    tolerance: {
      up: 0,
      down: 5
    },
    classes: {
      initial: 'iheader',
      pinned: 'iheader--pinned',
      unpinned: 'iheader--unpinned',
      top: 'iheader--top',
      notTop: 'iheader--not-top',
      bottom: 'iheader--bottom',
      notBottom: 'iheader--not-bottom',
      frozen: 'iheader--frozen'
    }
  };
  var headroom = new (headroom_js__WEBPACK_IMPORTED_MODULE_0___default())(vars.intelHeader.get(0), options);
  headroom.init();
};

/***/ }),

/***/ "./src/js/frontend/components/topBannerHeight.js":
/*!*******************************************************!*\
  !*** ./src/js/frontend/components/topBannerHeight.js ***!
  \*******************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   topBannerHeight: function() { return /* binding */ topBannerHeight; }
/* harmony export */ });
/* harmony import */ var _utils__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../utils */ "./src/js/frontend/utils.js");
/**
 * Handles the header search functionality, including opening the search modal and
 * closing it when clicking on the overlay or close button.
 *
 * @param {Object} $    - The jQuery object.
 * @param {Object} vars - An object containing the necessary variables and elements.
 */


var topBannerHeight = function topBannerHeight($, vars) {
  if (!(0,_utils__WEBPACK_IMPORTED_MODULE_0__.elementExists)(vars.topBannerSpacer)) {
    return false;
  }
  var headerHeight = vars.intelHeader[0].getBoundingClientRect().height;
  var viewportHeight = window.innerHeight;
  var bannerHeight = viewportHeight - headerHeight + 20;
  vars.topBannerSpacer.height("".concat(bannerHeight, "px"));
};

/***/ }),

/***/ "./src/js/frontend/components/variables.js":
/*!*************************************************!*\
  !*** ./src/js/frontend/components/variables.js ***!
  \*************************************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   initVars: function() { return /* binding */ initVars; }
/* harmony export */ });
/**
 * Init variables
 *
 * @param {jQuery} $ - The jQuery object.
 */

var initVars = function initVars($) {
  return {
    window: $(window),
    document: $(document),
    body: $('body'),
    mainMenu: $('.sf-menu'),
    handheldMenu: $('.alsiha-handheld-menu'),
    intelHeader: $('.intelligent-header'),
    preLoader: $('.alsiha-site-preloader'),
    toTop: $('.alsiha-scroll-to-top'),
    headerSpace: $('.fixed-header-space'),
    overlay: $('.alsiha-body-overlay'),
    topBannerSpacer: $('.top-banner-spacer'),
    headerSearchModal: $('#header-search'),
    headerSearchTrigger: $('header .search-trigger'),
    exists: function exists(el) {
      return el.length > 0;
    }
  };
};

/***/ }),

/***/ "./src/js/frontend/utils.js":
/*!**********************************!*\
  !*** ./src/js/frontend/utils.js ***!
  \**********************************/
/***/ (function(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   elementExists: function() { return /* binding */ elementExists; }
/* harmony export */ });
/**
 * Utility functions for common tasks in the project.
 *
 * @module utils
 */

/**
 * Checks if a jQuery element exists (i.e., its length is greater than 0).
 *
 * @param {jQuery} $element - The jQuery element to check.
 * @return {boolean} True if the element exists, false otherwise.
 */
var elementExists = function elementExists($element) {
  return $element.length > 0;
};

/***/ }),

/***/ "./node_modules/headroom.js/dist/headroom.js":
/*!***************************************************!*\
  !*** ./node_modules/headroom.js/dist/headroom.js ***!
  \***************************************************/
/***/ (function(module) {

/*!
 * headroom.js v0.12.0 - Give your page some headroom. Hide your header until you need it
 * Copyright (c) 2020 Nick Williams - http://wicky.nillia.ms/headroom.js
 * License: MIT
 */

(function (global, factory) {
   true ? module.exports = factory() :
  0;
}(this, function () { 'use strict';

  function isBrowser() {
    return typeof window !== "undefined";
  }

  /**
   * Used to detect browser support for adding an event listener with options
   * Credit: https://developer.mozilla.org/en-US/docs/Web/API/EventTarget/addEventListener
   */
  function passiveEventsSupported() {
    var supported = false;

    try {
      var options = {
        // eslint-disable-next-line getter-return
        get passive() {
          supported = true;
        }
      };
      window.addEventListener("test", options, options);
      window.removeEventListener("test", options, options);
    } catch (err) {
      supported = false;
    }

    return supported;
  }

  function isSupported() {
    return !!(
      isBrowser() &&
      function() {}.bind &&
      "classList" in document.documentElement &&
      Object.assign &&
      Object.keys &&
      requestAnimationFrame
    );
  }

  function isDocument(obj) {
    return obj.nodeType === 9; // Node.DOCUMENT_NODE === 9
  }

  function isWindow(obj) {
    // `obj === window` or `obj instanceof Window` is not sufficient,
    // as the obj may be the window of an iframe.
    return obj && obj.document && isDocument(obj.document);
  }

  function windowScroller(win) {
    var doc = win.document;
    var body = doc.body;
    var html = doc.documentElement;

    return {
      /**
       * @see http://james.padolsey.com/javascript/get-document-height-cross-browser/
       * @return {Number} the scroll height of the document in pixels
       */
      scrollHeight: function() {
        return Math.max(
          body.scrollHeight,
          html.scrollHeight,
          body.offsetHeight,
          html.offsetHeight,
          body.clientHeight,
          html.clientHeight
        );
      },

      /**
       * @see http://andylangton.co.uk/blog/development/get-viewport-size-width-and-height-javascript
       * @return {Number} the height of the viewport in pixels
       */
      height: function() {
        return win.innerHeight || html.clientHeight || body.clientHeight;
      },

      /**
       * Gets the Y scroll position
       * @return {Number} pixels the page has scrolled along the Y-axis
       */
      scrollY: function() {
        if (win.pageYOffset !== undefined) {
          return win.pageYOffset;
        }

        return (html || body.parentNode || body).scrollTop;
      }
    };
  }

  function elementScroller(element) {
    return {
      /**
       * @return {Number} the scroll height of the element in pixels
       */
      scrollHeight: function() {
        return Math.max(
          element.scrollHeight,
          element.offsetHeight,
          element.clientHeight
        );
      },

      /**
       * @return {Number} the height of the element in pixels
       */
      height: function() {
        return Math.max(element.offsetHeight, element.clientHeight);
      },

      /**
       * Gets the Y scroll position
       * @return {Number} pixels the element has scrolled along the Y-axis
       */
      scrollY: function() {
        return element.scrollTop;
      }
    };
  }

  function createScroller(element) {
    return isWindow(element) ? windowScroller(element) : elementScroller(element);
  }

  /**
   * @param element EventTarget
   */
  function trackScroll(element, options, callback) {
    var isPassiveSupported = passiveEventsSupported();
    var rafId;
    var scrolled = false;
    var scroller = createScroller(element);
    var lastScrollY = scroller.scrollY();
    var details = {};

    function update() {
      var scrollY = Math.round(scroller.scrollY());
      var height = scroller.height();
      var scrollHeight = scroller.scrollHeight();

      // reuse object for less memory churn
      details.scrollY = scrollY;
      details.lastScrollY = lastScrollY;
      details.direction = scrollY > lastScrollY ? "down" : "up";
      details.distance = Math.abs(scrollY - lastScrollY);
      details.isOutOfBounds = scrollY < 0 || scrollY + height > scrollHeight;
      details.top = scrollY <= options.offset[details.direction];
      details.bottom = scrollY + height >= scrollHeight;
      details.toleranceExceeded =
        details.distance > options.tolerance[details.direction];

      callback(details);

      lastScrollY = scrollY;
      scrolled = false;
    }

    function handleScroll() {
      if (!scrolled) {
        scrolled = true;
        rafId = requestAnimationFrame(update);
      }
    }

    var eventOptions = isPassiveSupported
      ? { passive: true, capture: false }
      : false;

    element.addEventListener("scroll", handleScroll, eventOptions);
    update();

    return {
      destroy: function() {
        cancelAnimationFrame(rafId);
        element.removeEventListener("scroll", handleScroll, eventOptions);
      }
    };
  }

  function normalizeUpDown(t) {
    return t === Object(t) ? t : { down: t, up: t };
  }

  /**
   * UI enhancement for fixed headers.
   * Hides header when scrolling down
   * Shows header when scrolling up
   * @constructor
   * @param {DOMElement} elem the header element
   * @param {Object} options options for the widget
   */
  function Headroom(elem, options) {
    options = options || {};
    Object.assign(this, Headroom.options, options);
    this.classes = Object.assign({}, Headroom.options.classes, options.classes);

    this.elem = elem;
    this.tolerance = normalizeUpDown(this.tolerance);
    this.offset = normalizeUpDown(this.offset);
    this.initialised = false;
    this.frozen = false;
  }
  Headroom.prototype = {
    constructor: Headroom,

    /**
     * Start listening to scrolling
     * @public
     */
    init: function() {
      if (Headroom.cutsTheMustard && !this.initialised) {
        this.addClass("initial");
        this.initialised = true;

        // defer event registration to handle browser
        // potentially restoring previous scroll position
        setTimeout(
          function(self) {
            self.scrollTracker = trackScroll(
              self.scroller,
              { offset: self.offset, tolerance: self.tolerance },
              self.update.bind(self)
            );
          },
          100,
          this
        );
      }

      return this;
    },

    /**
     * Destroy the widget, clearing up after itself
     * @public
     */
    destroy: function() {
      this.initialised = false;
      Object.keys(this.classes).forEach(this.removeClass, this);
      this.scrollTracker.destroy();
    },

    /**
     * Unpin the element
     * @public
     */
    unpin: function() {
      if (this.hasClass("pinned") || !this.hasClass("unpinned")) {
        this.addClass("unpinned");
        this.removeClass("pinned");

        if (this.onUnpin) {
          this.onUnpin.call(this);
        }
      }
    },

    /**
     * Pin the element
     * @public
     */
    pin: function() {
      if (this.hasClass("unpinned")) {
        this.addClass("pinned");
        this.removeClass("unpinned");

        if (this.onPin) {
          this.onPin.call(this);
        }
      }
    },

    /**
     * Freezes the current state of the widget
     * @public
     */
    freeze: function() {
      this.frozen = true;
      this.addClass("frozen");
    },

    /**
     * Re-enables the default behaviour of the widget
     * @public
     */
    unfreeze: function() {
      this.frozen = false;
      this.removeClass("frozen");
    },

    top: function() {
      if (!this.hasClass("top")) {
        this.addClass("top");
        this.removeClass("notTop");

        if (this.onTop) {
          this.onTop.call(this);
        }
      }
    },

    notTop: function() {
      if (!this.hasClass("notTop")) {
        this.addClass("notTop");
        this.removeClass("top");

        if (this.onNotTop) {
          this.onNotTop.call(this);
        }
      }
    },

    bottom: function() {
      if (!this.hasClass("bottom")) {
        this.addClass("bottom");
        this.removeClass("notBottom");

        if (this.onBottom) {
          this.onBottom.call(this);
        }
      }
    },

    notBottom: function() {
      if (!this.hasClass("notBottom")) {
        this.addClass("notBottom");
        this.removeClass("bottom");

        if (this.onNotBottom) {
          this.onNotBottom.call(this);
        }
      }
    },

    shouldUnpin: function(details) {
      var scrollingDown = details.direction === "down";

      return scrollingDown && !details.top && details.toleranceExceeded;
    },

    shouldPin: function(details) {
      var scrollingUp = details.direction === "up";

      return (scrollingUp && details.toleranceExceeded) || details.top;
    },

    addClass: function(className) {
      this.elem.classList.add.apply(
        this.elem.classList,
        this.classes[className].split(" ")
      );
    },

    removeClass: function(className) {
      this.elem.classList.remove.apply(
        this.elem.classList,
        this.classes[className].split(" ")
      );
    },

    hasClass: function(className) {
      return this.classes[className].split(" ").every(function(cls) {
        return this.classList.contains(cls);
      }, this.elem);
    },

    update: function(details) {
      if (details.isOutOfBounds) {
        // Ignore bouncy scrolling in OSX
        return;
      }

      if (this.frozen === true) {
        return;
      }

      if (details.top) {
        this.top();
      } else {
        this.notTop();
      }

      if (details.bottom) {
        this.bottom();
      } else {
        this.notBottom();
      }

      if (this.shouldUnpin(details)) {
        this.unpin();
      } else if (this.shouldPin(details)) {
        this.pin();
      }
    }
  };

  /**
   * Default options
   * @type {Object}
   */
  Headroom.options = {
    tolerance: {
      up: 0,
      down: 0
    },
    offset: 0,
    scroller: isBrowser() ? window : null,
    classes: {
      frozen: "headroom--frozen",
      pinned: "headroom--pinned",
      unpinned: "headroom--unpinned",
      top: "headroom--top",
      notTop: "headroom--not-top",
      bottom: "headroom--bottom",
      notBottom: "headroom--not-bottom",
      initial: "headroom"
    }
  };

  Headroom.cutsTheMustard = isSupported();

  return Headroom;

}));


/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/compat get default export */
/******/ 	!function() {
/******/ 		// getDefaultExport function for compatibility with non-harmony modules
/******/ 		__webpack_require__.n = function(module) {
/******/ 			var getter = module && module.__esModule ?
/******/ 				function() { return module['default']; } :
/******/ 				function() { return module; };
/******/ 			__webpack_require__.d(getter, { a: getter });
/******/ 			return getter;
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/define property getters */
/******/ 	!function() {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = function(exports, definition) {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	!function() {
/******/ 		__webpack_require__.o = function(obj, prop) { return Object.prototype.hasOwnProperty.call(obj, prop); }
/******/ 	}();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	!function() {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = function(exports) {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	}();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry need to be wrapped in an IIFE because it need to be in strict mode.
!function() {
"use strict";
/*!*************************************!*\
  !*** ./src/js/frontend/frontend.js ***!
  \*************************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _classes_SigmaFrontend__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./classes/SigmaFrontend */ "./src/js/frontend/classes/SigmaFrontend.js");
/**
 * Frontend JS.
 */





/**
 * Initializes the SigmaFrontend class and calls its methods on DOM ready and window load events
 *
 * @function
 * @param {jQuery} $ - jQuery object
 */
(function ($) {
  var sigmaFrontend = new _classes_SigmaFrontend__WEBPACK_IMPORTED_MODULE_0__.SigmaFrontend();

  /**
   * Calls the `domReady()` method of the `sigmaFrontend` object when the DOM is ready
   *
   * @function
   */
  $(function () {
    sigmaFrontend.domReady();
  });

  /**
   * Calls the `domLoad()` method of the `sigmaFrontend` object when the window is loaded
   *
   * @function
   */
  $(window).on('load', function () {
    sigmaFrontend.domLoad();
  });

  /**
   * Calls the `domResize()` method of the `sigmaFrontend` object when the window is resized
   *
   * @function
   */
  $(window).on('resize', function () {
    sigmaFrontend.domResize();
  });
})(jQuery);
}();
/******/ })()
;
//# sourceMappingURL=frontend.min.js.map